## Codebase Patterns (Phase 2B, presentation-format handlers)

- Pandoc helpers (ensure-pandoc.sh, render-pdf.sh, source-hash.sh) from Phase 2A are the substrate — inherit them, don't reinvent.
- Page-selection logic (tag / folder / file / `all` + deterministic sort) is duplicated across generate-book and generate-slides. If generate-mindmap or generate-infographic makes it a third copy, lift it into `.claude/skills/generate/lib/select-pages.sh`.
- Wikilink sed pass is the exact same one-liner in every handler — another candidate for the shared lib/.
- Marp CLI installs via `pnpm add -g` or `npm install -g`; fall back to `npx @marp-team/marp-cli` if install fails. No brew formula.
- Vault-local template overrides live at `<vault>/.templates/<type>/` — lets vaults ship house style without polluting the shared handler.

---

## 2026-04-18 10:25 - US-001: Refactor /slides into generate-slides handler

- Created `.claude/skills/generate-slides/SKILL.md` — Marp default, `--format reveal` opt-in, artifacts/slides/ output with HTML + PDF + .marp.md + .meta.yaml sidecar.
- Added `.claude/skills/generate-slides/templates/default.md` — minimal Marp template with `{{title}} {{author}} {{date}} {{source_count}} {{body}}` placeholders.
- Reduced `.claude/skills/slides/SKILL.md` to a deprecation shim — prints redirect, hands off to generate-slides with the same args.
- Matches the established Phase 2A pattern exactly: same 8-step pipeline (deps → resolve → hash → build → render → sidecar → commit → report), `user-invocable: false`, sidecar schema from reference/artifacts.md.
- **Learnings for future iterations:**
  - The `---` slide separator in Marp collides with YAML frontmatter — safe because frontmatter only has one leading pair, but worth documenting in the handler.
  - Marp does not auto-paginate — slides with long bodies overflow silently. Handler documents a 12-line soft rule and "(cont.)" continuation slide pattern.
  - Mermaid blocks still aren't rendered in slides (same limitation as book/pdf in Phase 2A). Phase 2B sibling story should add a `mermaid-cli → PNG` pre-pass that all handlers can share.
  - Reveal.js PDF is intentionally omitted — browser-print-to-PDF is brittle; keep scope tight.
  - Two more duplications spotted (page-selection + wikilink sed) — worth promoting to `.claude/skills/generate/lib/` when US-002 or US-003 touches them.
---

## 2026-04-18 11:34 - US-002: Implement generate-mindmap handler

- Created `.claude/skills/generate-mindmap/SKILL.md` — markmap-cli renderer (lazy install via pnpm/npm fallback), mermaid-mindmap fallback when install fails. Output: `.outline.md` (markmap source) + `.html` (self-contained rendered mindmap) + `.meta.yaml` sidecar.
- Added `.claude/skills/generate-mindmap/templates/default.md` with `markmap:` YAML config block (colorFreezeLevel, maxWidth, initialExpandLevel, spacing).
- Outline construction rule: H1 as root label, H2 as top branches, H3 as sub-branches, H4+ flattened. Wikilinks render as italic labels; `--include-wikilinks` adds a "Related" sub-branch. Each page ends with a `[Source]()` link to the original wiki `.md`.
- Documented Obsidian embedding via HTML Reader plugin + `<iframe>` dataviewjs snippet for the .html output.
- **Learnings for future iterations:**
  - Page-selection logic is now duplicated THREE times (book, slides, mindmap) — `.claude/skills/generate/lib/select-pages.sh` is now overdue. Extract on US-003 (generate-infographic) to avoid a fourth copy.
  - Wikilink sed pass also at 3x — same extraction opportunity into `lib/resolve-wikilinks.sh`.
  - Markmap produces self-contained HTML (inline SVG + JS) — no hosting concern, no CDN dependency. Good fit for the artifacts-are-portable principle.
  - Mermaid-mindmap fallback gives us graceful degradation when npm install is blocked — same pattern as generate-book's HTML-fallback-when-LaTeX-missing.
  - Markmap does NOT support cross-file hyperlinks to other markmaps; node-level links only work for external URLs or local file paths. Documented as a limitation.
  - The `markmap:` YAML block inside the outline.md is markmap-specific (not standard frontmatter) — markmap-cli parses it; other tools ignore it.
---

## 2026-04-18 11:38 - US-003: Implement generate-infographic handler + extract shared helpers

- Created `.claude/skills/generate/lib/select-pages.sh` — single source of truth for topic-to-pages resolution (all / folder / file / tag). Exit 1 on empty match.
- Created `.claude/skills/generate/lib/resolve-wikilinks.sh` — sed one-liner that renders `[[page]]` / `[[page|text]]` as italic. Pipe through this instead of copying the pattern.
- Created `.claude/skills/generate-infographic/SKILL.md` — SVG-template handler with auto-pick (comparison-grid if "vs"/"versus" in topic OR ≥2 comparison pages, else stats-card). Optional PNG export via rsvg-convert (brew install librsvg).
- Shipped 2 Observatory-themed SVG templates:
  - `templates/stats-card.svg` — 1600×900, header band amber, 3 stat cards (pages cyan, tags amber, top concepts green), tag strip, pull-quote panel. Uses `<foreignObject>` for text wrapping.
  - `templates/comparison-grid.svg` — 1600×1000, two columns (cyan left, green right) with 4 dimension rows + amber verdict band. Also uses foreignObject for long text.
- Handler documents the `{{placeholder}}` convention and the Observatory palette table; new templates must stay on-brand or note deviations.
- **Learnings for future iterations:**
  - Extraction into `generate/lib/` was overdue — did it at the 4th-use point (would-be). The older handlers (book, slides, mindmap) still have inline copies; they can migrate lazily on next touch. No forced refactor — the inline code still works.
  - SVG `<foreignObject>` with embedded HTML is essential for multi-line text that wraps — pure `<text>` elements don't wrap. Works in all modern SVG renderers including rsvg-convert.
  - The LLM-fills-template pattern here is fragile: if the invoking LLM produces bad data (empty strings, malformed SVG fragments) the output silently breaks. Future close-the-loop testing (Phase 2E) should validate SVG parses as XML.
  - `rsvg-convert` is a better bet than Inkscape for headless PNG export — smaller dependency, scriptable, consistent output. brew install librsvg.
  - 2-template shipping target from PRD was met; timeline template intentionally deferred to Phase 2C/2E.
---
